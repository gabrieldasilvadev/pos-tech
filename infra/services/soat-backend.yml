apiVersion: v1
kind: Service
metadata:
  name: soat-backend
  namespace: soat
  labels:
    app: soat-backend
    tier: backend
  annotations:
    maintainer: "SOAT - Grupo 75"
    description: "Expor a API do backend via LoadBalancer"
spec:
  selector:
    app: soat-backend
  ports:
    - port: 80
      targetPort: 8080
  type: LoadBalancer
