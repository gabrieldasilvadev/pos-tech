spring:
  datasource:
    url: jdbc:tc:postgresql:15.3:///soat?TC_INITSCRIPT=db/migration/V202505241714__create_payments_table.sql
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

